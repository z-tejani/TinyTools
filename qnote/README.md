# Quick Note

A tiny script that lets you write a quick note from your terminal which it then appends, with a timestamp, to a central `notes.txt` file.

## Usage

Add a note:
```bash
python3 qnote.py "Remember to buy milk"
```

List all notes:
```bash
python3 qnote.py
```
