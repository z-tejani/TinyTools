# URL Shortener CLI

A script that takes a long URL as input and uses the TinyURL API to return a shortened link.

## Requirements

- Python 3
- `requests` (`pip install requests`)

## Usage

```bash
python3 shortener.py <long_url>
```
