# Dictionary CLI

A script where you type `define <word>` (or just pass the word as an argument) and it hits a free dictionary API to give you the definition.

## Usage

```bash
python3 dictionary.py <word>
```
