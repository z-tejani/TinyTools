# HTML to Text

A tool that scrapes a URL or reads an HTML file and strips all HTML tags, outputting just the plain text content.

## Requirements

- Python 3
- `requests` (`pip install requests`)
- `beautifulsoup4` (Optional but recommended: `pip install beautifulsoup4`)

## Usage

```bash
python3 html2text.py <url_or_file>
```
