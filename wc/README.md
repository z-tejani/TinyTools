# WC (Word Count)

A simple clone of the `wc` command written in Go. It takes a file as input and returns the number of words, lines, and characters (bytes).

## Usage

```bash
go run wc.go <filename>
```

or build it:

```bash
go build -o mywc wc.go
./mywc <filename>
```
