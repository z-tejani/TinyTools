# CSV to JSON Converter

A script that reads a .csv file and converts it into a .json file (array of objects).

## Usage

```bash
python3 csv2json.py input.csv [output.json]
```

If the output filename is omitted, it will use the same name as the input file with a `.json` extension.
