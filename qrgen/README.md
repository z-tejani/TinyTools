# QR Code Generator

A script that takes text or a URL as input and generates a QR code image file.

## Requirements

- Python 3
- `qrcode` (`pip install qrcode[pil]`)

## Usage

```bash
python3 qrgen.py "https://example.com" [--output myqr.png]
```
